# MLCE-texture-pack-builder
Simple python script to make your custom texture packs for Minecraft Community Legacy Edition.

I provived an example pack, keep the file structure the same, and png sizes the same
the game should accept x32 textures, but i'll make the script generate them in a future commit

just drag the source folder on the script and move what's inside the output in a subfolder of your DLC folder
